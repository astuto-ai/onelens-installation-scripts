apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "onelens-agent.fullname" . }}-env
  labels:
    {{- include "onelens-agent.labels" . | nindent 4 }}
data:
  {{- toYaml .Values.env | nindent 2 }}