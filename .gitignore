# Internal documentation - not for public repo
internalwiki/

# Build artifact — generated by scripts/build-patching.sh from src/patching.sh
# Committed only on release tags by CI, not during development.
# To regenerate: bash scripts/build-patching.sh
/patching.sh

# Build output directory (legacy)
dist/
