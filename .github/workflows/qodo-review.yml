name: Qodo Code Review (Merge Gate)
on:
  pull_request:
    branches: ["main", "master"]
    types: [opened, reopened, ready_for_review, synchronize]
  workflow_dispatch:
    inputs:
      pr_number:
        description: "PR number to review"
        required: true

concurrency:
  group: qodo-review-${{ github.event.pull_request.number || github.event.inputs.pr_number }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  review:
    if: |
      (github.event_name == 'pull_request' && github.event.pull_request.draft == false) ||
      (github.event_name == 'workflow_dispatch')
    # NOTE: this repo is public and astuto-ai/.github is internal, so GitHub
    # forbids calling the org reusable workflow directly. Keep this local copy
    # in sync with astuto-ai/.github/.github/workflows/qodo-review.yml — identical
    # except this repo's OPENAI secret is named OPENAI_API_KEY (org uses OPENAI_KEY).
    uses: ./.github/workflows/qodo-review-reusable.yml
    secrets: inherit
